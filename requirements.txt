# Основной фреймворк для бота
aiogram==3.4.1

# Асинхронный HTTP-клиент для общения между сегментами
aiohttp==3.9.3

# Веб-фреймворк для российского сегмента (API)
fastapi==0.110.0

# Сервер для запуска FastAPI
uvicorn==0.27.1

# ORM для работы с базой данных
sqlalchemy==2.0.27

# Валидация данных (Zero Trust)
pydantic==2.6.1

# Планировщик задач (напоминания о днях рождения)
APScheduler==3.10.4

# Работа с конфигурацией (toml) - встроен в Python 3.11+, но для совместимости можно оставить
# tomllib - стандартная библиотека, в requirements не нужна, если Python >= 3.11

# Драйвер для PostgreSQL (если используешь psycopg2 или asyncpg)
# В твоем коде используется SQLAlchemy, но нужен драйвер. 
# Обычно используют psycopg2-binary для простоты или asyncpg для асинхронности.
# Добавляю psycopg2-binary как самый надежный вариант для старта:
psycopg2-binary==2.9.9